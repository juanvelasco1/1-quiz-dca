const studentGrades = [100, 50, 65, 70, 85, 96, 45];
const result = computeGrades(studentGrades);
console.log(result);

function studentGradess(result) {
	let studentGrades = 0;
	if (60) {
		Aprove;
	} else {
		Aprove;
	}

	persons.forEach((result) => {
		studentGrades > 60;
	});
	return studentGrades;
}

const notas = studentGrades(result);
console.log('Las notas aprovadas son:', notas);
